spring.application.name=Spring Boot Coding
spring.h2.console.enabled=true
# Use H2 file-based database ? include the 'file:' prefix so the JDBC URL is valid on Windows
spring.datasource.url=jdbc:h2:file:C:/Users/admin/Documents/Spring Boot Coding/db
spring.datasource.driver-class-name=org.h2.Driver
# Use default H2 credentials (or change as needed)
spring.datasource.username=aman
spring.datasource.password=amansaeed
spring.jpa.hibernate.ddl-auto=update
